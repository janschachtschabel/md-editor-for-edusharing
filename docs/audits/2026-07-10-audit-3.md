# Voll-Audit mit kompletter Problemliste (6. Durchlauf, 2026-07-10, abends)

**Auftrag:** gesamten Code gründlich prüfen, vollständige Fehler-/Problemliste
(kurz: was + wo). **Level: Deep** — frische Scanner über alles, zeilenweise
Lektüre sämtlicher `src/`- und `server/`-Module (inkl. der zuletzt nur im
Delta geprüften: edu-sharing-api, host, UI-Module), Hypothesen empirisch
verifiziert statt behauptet. **Read-only.**

## 1. Executive Summary

Nach fünf Audit-/Fix-Runden ist der Zustand der beste der Projektgeschichte:
**alle Scanner makellos** (0 CVEs, 279 Checks/0 Fails, Lint 0, keine Zirkel,
depcheck sauber, nichts outdated, Compose valide) und **kein Finding oberhalb
🟡**. Übrig bleiben 3 mittlere Punkte (ungetestete UI-Schicht, zwei Dateien
über dem Größenrichtwert, fehlendes Kosten-Limit am KI-Kommando) und 8 kleine
— darunter ein frisch **empirisch belegter** Randfall im KI-Rollen-Anker
(fehlender Block-Trenner in `ytextOf`). Wichtigste Einzelmaßnahme, falls man
eine wählt: automatisierte Tests für die Web-Component-Schicht (T-1).

## 2. Scanner & Kennzahlen

```
npm audit   0 vulnerabilities     npm outdated  – (leer)      depcheck  clean
lint        0                     tests         11 Suiten · 279 Checks · 0 FAIL
build       ok                    madge         keine Zirkel  compose   VALID
Größen >300 Z.: md-collab-editor.js 462 · i18n.js 389 (Sprachkatalog) ·
                collab.js 374 · host.js 320 (Demo-Host)
```

## 3. Vollständige Problemliste (kurz: was + wo)

Keine 🔴- oder 🟠-Findings. Vollständige Liste aller belegten Punkte:

| ID | Sev | Wo | Was | Fix (kurz) |
|---|---|---|---|---|
| T-1 | 🟡 | `test/` (Fehlen); betrifft `src/md-collab-editor.js`, `src/role-ui.js`, `src/presence.js`, `src/host.js` | Die Web-Component-Schicht hat keine automatisierten Tests — nur Live-Verifikation + jsdom für annotation-ui. Toolbar-Wiring, Save-Bar, Rollen-Select, session-expired-Fluss ungetestet | jsdom-Harness für die Komponente (Provider gemockt) — der teuerste offene Punkt (~1–2 Tage) |
| M-2 | 🟡 | `src/md-collab-editor.js` (462 Z.), `src/host.js` (320 Z.) | Weiter über dem 300-Zeilen-Richtwert (M-1-Rest; host ist Demo-Code) | nächste Extraktion: Save-Bar-Wiring; host bewusst tolerieren |
| P-1 | 🟡 | `server/collab.js:378` (onStateless `ai-tag`) | Außer Busy-Lock kein Rate-/Kosten-Limit: ein schreibberechtigter Nutzer kann beliebig viele Modell-Calls auslösen (Token-Kosten) | Per-User-Cooldown (z. B. 30 s) oder Tageslimit je Dokument |
| L-1 | 🟢 | `server/ai-tagging.js:146` (`ytextOf`) | **Empirisch belegt:** konkateniert Blockkinder ohne Trenner — `ytextOf(blockquote)` = „Ende eins.Zwei Anfang." → ein (vom Modell so geliefertes) grenzüberschreitendes Zitat matcht fälschlich → falscher Rollen-Wrap eines Mehr-Absatz-Blockquotes möglich | `'\n'` zwischen Element-Kindern einfügen (Zitate enthalten nie `\n`) |
| L-2 | 🟢 | `server/collab.js:201 ff.` (Prune nach Save) | Server-Prune markiert das Dokument erneut dirty → ein Folge-Noop-Save + kurzer Countdown-Blip bei allen Clients | Server-Transaktion mit Origin markieren und in onChange ignorieren |
| P-2 | 🟢 | `src/role-ui.js:60–65` (`renderChips`) | Läuft bei jedem Doc-Update: voller `descendants`-Walk + DOM-Rebuild, auch wenn 0 Rollen existieren (kein Early-Exit wie beim Entities-Pendant F-T4) | Early-Exit, wenn erste Traversierung nichts findet, bzw. Rollen-Count cachen |
| P-3 | 🟢 | `src/md-collab-editor.js:226` (`_saveTicker`) | Save-Bar wird sekündlich neu gerendert, auch im Idle-Zustand ohne Countdown | Ticker nur bei `dirty`/pending laufen lassen |
| C-1 | 🟢 | `server/config.js:29` | `SAVE_RETRY_MS = 30000` ist der einzige Timing-Wert ohne Env-Override (Inkonsistenz zu SAVE_DEBOUNCE_MS etc.) | `Number(process.env.SAVE_RETRY_MS \|\| 30000)` + Doku-Zeile |
| D-1 | 🟢 | `Dockerfile:6` (`RUN npm ci`) | Produktions-Image enthält devDependencies (esbuild, eslint) — kein Multi-Stage-Build (offen seit Audit 4) | Multi-Stage: Build-Stage mit devDeps, Final-Stage `npm ci --omit=dev` |
| A-1 | 🟢 | `public/index.html:51` (`#f-name`) | Anzeigename-Input ohne `autocomplete="name"` (die Login-Felder haben ihre autocomplete-Attribute) | Attribut ergänzen |
| O-1 | 🟢 | `server/ai-tagging.js` (buildPrompt/Design) | Beobachtung: Dokumenttext geht ungefiltert als User-Message ans Modell — Prompt-Injection im Text kann die Vorschlagsqualität verzerren. Schaden strukturell begrenzt (nur existierende Zitate, Rollen nur aus Katalog, Write-Gate, Busy-Lock, kein Auto-Apply ins Repo ohne Save-Regeln) | akzeptiert; im Briefing/Doku als bekannte Eigenschaft führen |

**Bewusste, dokumentierte Tickets (keine Findings):**
`@hocuspocus/extension-database` + Redis-Session-Store für Multi-Instanz-Betrieb ·
Ticket-Login bislang nur gegen Mock verifiziert (echtes Staging-Ticket braucht
die reale Einbettung) · `i18n.js` 389 Z. = reiner Sprachkatalog (Daten, kein Code-Smell).

## 4. Scorecard

```
Architektur 90 · Security 90 · Logik/Korrektheit 90 · Performance 86
API 88 · Frontend/UX 86 · Testing 85 · Wartbarkeit 84 · Dependencies 95 · DevOps 88
Dokumentation 92 · Datenbank N/A
Overall: 89 (gewichtet)   — Verlauf: 78 → 81 → 86 → 88 → 85 → 89
```

- **Security 90:** fünffach auditiert; Auth/Logout/Gates hook- und live-getestet;
  einziger Rest ist das Kosten- (nicht Sicherheits-) Thema P-1.
- **Logik 90:** KW-1 vollständig geschlossen (4 Pfade + Regressionstests);
  Rest sind zwei belegte 🟢-Randfälle (L-1, L-2).
- **Testing 85:** 279 Checks decken Logik + Server-Persistenz + Security-Gates
  stark; die Komponenten-Schicht bleibt die eine echte Lücke (T-1).
- **Dependencies 95:** 0 CVEs, nichts outdated, keine ungenutzten, alles permissiv.
- **Wartbarkeit 84:** zwei Dateien über Richtwert (M-2), sonst saubere Module.

## 5. Verdict: **Ship** ✅

Keine Blocker, keine 🟠-Findings. Alle Kernpfade (Kollaboration, Persistenz
mit Read-Back, beide Tagging-Systeme, KI, Auth/Logout inkl. Session-Kill,
i18n, Deployment) sind produktionsreif und regressionsgesichert. Die drei
🟡-Punkte sind Qualitäts-Invest, nicht Release-Hindernis.

## 6. Quick Wins

- **<10 min:** C-1 (Env-Override), A-1 (autocomplete), L-1 (`'\n'`-Join in ytextOf)
- **<30 min:** P-2 (Early-Exit), P-3 (Idle-Ticker), D-1 (Multi-Stage-Dockerfile)
- **<2 h:** P-1 (KI-Cooldown), L-2 (Origin-markierte Server-Transaktion)

## 7. Tech-Debt

| Kategorie | Stand | Aufwand | Risiko | Zinseszins |
|---|---|---|---|---|
| Security | keine bekannte Schuld | – | – | – |
| Architektur | M-2 (2 Dateien über Richtwert) | klein | niedrig | leicht (wächst je Feature) |
| Test | T-1 (Komponenten-Schicht) | mittel (1–2 T) | mittel | **ja** — jede UI-Änderung erfordert manuelle Verifikation |
| Dependencies | keine | – | – | – |
| Doku | keine (6× synchronisiert) | – | – | – |
| Infrastruktur | D-1 + bewusste Tickets (DB/Redis) | klein / mittel | niedrig / mittel (erst bei Multi-Instanz) | nein |

Priorisieren: **T-1** — die einzige Schuld mit Zinseszins.

## 8. Roadmap

```
Phase 1 (Quick Wins, <½ Tag):  L-1, C-1, A-1, P-2, P-3, D-1
Phase 2 (Qualität, 1–2 Tage):  T-1 Komponenten-Testharness (jsdom + Mock-Provider)
Phase 3 (bei Bedarf):          P-1 KI-Cooldown · L-2 Origin-Markierung ·
                               extension-database/Redis (erst bei Multi-Instanz) ·
                               echtes Staging-Ticket (mit der realen Einbettung)
```

*Erstellt mit `/better-coding-audit` gegen den Quellstand 2026-07-10 (abends);
read-only, alle Findings quellzeilen-belegt, L-1 per Repro-Skript verifiziert.*
