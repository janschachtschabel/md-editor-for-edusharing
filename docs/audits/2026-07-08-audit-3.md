# Abschluss-Audit (3. Durchlauf, 2026-07-08)

**Level:** Deep — Scanner frisch, alle seit Audit 2 geänderten Stellen zeilenweise,
plus vollständige Lektüre der übrigen Module (server.js, sessions.js, guards.js,
config.js, i18n.js, markdown.js, save-state.js, extensions.js, index.html,
Dockerfile). **Read-only — kein Code geändert.**
**Vorgänger:** [`2026-07-08-audit.md`](2026-07-08-audit.md) · [`2026-07-08-audit-2.md`](2026-07-08-audit-2.md)

## 1. Scanner (alle grün)

```
npm audit      0 vulnerabilities
npm run lint   0 Findings (ESLint flat config, alle Runtimes korrekt gemappt)
npm test       11/11 Suiten grün (inkl. der 2 neuen Server-Integrationstests)
npm run build  ok (beide Bundles)
madge          keine zirkulären Abhängigkeiten
npm outdated   nur TipTap-Patches 3.27.1 → 3.27.3 (Patch-Level) + Express 4 (Ticket)
```

## 2. Seit Audit 2 behoben und verifiziert

| Finding | Fix | Beweis |
|---|---|---|
| **F-T1** 🟠 Silenter Verlust parenthesierter Keywords | `consumed`/`preservedKeywords`/`mergeKeywords` — editor-verwaltet ist nur, was beim Laden als Annotation verankert wurde | `test/annotations.test.mjs` (F-T1-Regression) + `test/keyword-lifecycle.test.mjs` (17 Checks über den realen Persistenz-Pfad: Plain- und Fremd-Keywords überleben Entität-rein/raus/Reload) |
| **Duplikat-Lücke** 🟠 (Text/Pillen doppelt nach Reconnect ohne Änderungen) | Snapshot auch beim **Laden** (`rememberSnapshot` in onLoadDocument) + Cap 500 | `test/collab-load.test.mjs` — reproduzierte vor dem Fix exakt das Fehlerbild (109→218 Zeichen, 1→2 Pillen), jetzt grün |
| **Layout** 🟠 (Editor-Spalte rückt bei breiter Komponente zur Mitte) | `.mce-editor` `margin: 0` statt `0 auto` (Lesebreite 860px bleibt) | Live verifiziert: Editor-Linkskante = Pane-Linkskante bei 1920px |
| **Phantom-Keyword-Writes** 🟡 (Reorder galt als Änderung) | Change-Detection als Mengenvergleich (`sameList(..., unordered)`) | Lifecycle-Test: „no-op save writes nothing" |
| **Doppelte Pillen bei exakten Keyword-Duplikaten** 🟢 | Dedupe in `keywordsToAnnotations` | Testfall in annotations.test.mjs |
| **F-T2/S-1** (aus Audit 2 als offen geführt) | war **False Positive** — Basic-Passthrough-Limiter existiert und ist getestet | `api-auth.test.mjs` „S-1"-Checks; Nachtrag in Audit 2 |

Zusätzlich seit Audit 2 geprüft und **in Ordnung**: i18n (Key-Parität getestet,
Fallback-Kette, persistierte Werte sprachunabhängig, live DE/EN verifiziert),
Logout-Fluss (Komponente wird zerstört, Session serverseitig widerrufen),
Tagging-Integration (Standoff/Decorations/Y.Array, live gegen Staging gesehen).

## 3. Offene Punkte (alle 🟢 Low / bewusst)

- **DOC-1 — Testsuiten-Zahl driftet:** README.md nennt 6/7/9, README.en.md 9,
  CI-Kommentare (ci.yml, .gitlab-ci.yml) 9, ENTWICKLERLEITFADEN 6/7 — tatsächlich
  **11**. Empfehlung: Zahl aus den Doku-Fließtexten streichen („alle Suiten")
  oder einmal zentral korrigieren.
- **DEP-1 — TipTap 3.27.1 → 3.27.3** (Patch): gefahrloses `npm update`.
- **F-T4** — Decorations werden pro Tastendruck voll neu berechnet (erst bei
  sehr großen, stark getaggten Dokumenten relevant).
- **F-T5** — `md-collab-editor.js` über dem 300-Zeilen-Richtwert (Presence/
  Save-Bar-Wiring wären die nächsten Extraktionskandidaten).
- **F-T6** — programmatischer `add()`-Pfad prüft nicht auf blockübergreifende
  Zitate (der interaktive Pfad tut es).
- **Docker-Image enthält devDependencies** (esbuild/eslint im Final-Image, da
  Build im selben Stage): Multi-Stage-Build würde das Image verschlanken —
  Optimierung, kein Risiko.
- **Tickets (dokumentiert, bewusst):** Express 4→5; `@hocuspocus/extension-database`
  + Redis-Session-Store für Multi-Instanz-Produktion; Ticket-Login bislang nur
  gegen Mock verifiziert (echtes Staging-Ticket steht aus).

## 4. Scorecard

```
Architektur 88 · Security 86 · Logik/Korrektheit 88 · Performance 82
API 84 · Frontend/UX 86 · Testing 88 · Wartbarkeit 84 · Dependencies 90 · DevOps 86
Overall: 86 (gewichtet)   — Audit 1: ~78 · Audit 2: 81 · jetzt: 86
```

Begründung der Sprünge: Security +12 (S-1 als False Positive aufgeklärt — der
Limiter existiert und ist getestet), Logik +12 (F-T1 + Duplikat-Lücke + Phantom-
Writes behoben, jeweils mit Regressionstest), Testing +6 (der vormals ungetestete
Server-Persistenz-Pfad hat jetzt zwei Integrationssuiten mit gestubbtem Repo).

## 5. Verdict: **Ship** ✅

Keine offenen Findings oberhalb 🟢. Die drei heute behobenen 🟠-Probleme waren
die letzten produktionsrelevanten; alle Fixes sind regressionsgetestet. Für den
Produktivbetrieb gelten die dokumentierten Rahmenbedingungen (eine Instanz oder
extension-redis; `.env` nie ins Image; `TRUST_PROXY_HOPS` hinter Proxy setzen;
`ALLOW_ANONYMOUS_EDIT` nur lokal). Einzige empfohlene Rest-Aufgabe vor dem
nächsten Release: DOC-1 (Suiten-Zahl) und DEP-1 (TipTap-Patch) — beides Minuten.

*Erstellt mit `/better-coding-audit` gegen den Quellstand 2026-07-08 (nach der
Keyword-Lifecycle-Fix-Runde); read-only.*
