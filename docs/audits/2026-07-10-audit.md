# Abschluss-Audit (4. Durchlauf, 2026-07-10)

**Level:** Standard mit gezielten Deep-Reads — Scanner frisch; zeilenweise
geprüft wurden alle seit [Audit 3](2026-07-08-audit-3.md) neuen/geänderten
Teile: KI-Modul (`server/ai-tagging.js`, 239 Z.) inkl. Prompt/Batching/
Multi-Absatz-Rollen, KI-Verdrahtung (Komponente, collab.js, config.js,
docker-compose, .env.example), Absatzrollen (`role-block.js`, markdown-
Tokenizer), Presence-Extraktion, i18n-Erweiterungen, alle neuen Testsuiten.
**Read-only — kein Code geändert.**

## 1. Scanner (alle grün)

```
npm audit      0 vulnerabilities
npm run lint   0 Findings
npm test       11 Suiten, 271 Checks, 0 FAIL
npm run build  ok (beide Bundles)
madge          keine zirkulären Abhängigkeiten
compose        docker compose config → VALID
npm outdated   nur marked 18.0.5 → 18.0.6 (Patch)
```

## 2. Seit Audit 3 hinzugekommen — Qualitätsbefund

| Feature | Befund |
|---|---|
| **KI-Verschlagwortung** (🤖) | Vorbildlich gekapselt (ein Modul, 8 Zeilen Verdrahtung); Modellausgabe wird **strenger** validiert als Nutzereingaben (Zitat-Existenz, Kreuzung, Duplikate, Katalog-Rollen); Key nur serverseitig; **Read-only-Gate getestet** (echter Hook, beide Zweige) + Kette im Hocuspocus-Dist belegt; Busy-Lock, Stale-Vorschläge, Update-Batching getestet; 29-Check-Suite mit gestubbtem Modell; **live gegen echtes gpt-5.4-mini verifiziert** (Minimal-Span-Entitäten, mehrabsätzige Rollen inhaltsabhängig) |
| **Absatzrollen** (`:::`) | Zwei-System-Trennung sauber (Struktur im Text vs. Standoff-Keywords); fence-zählender Tokenizer für Verschachtelung; Auto-Split bei Teil-Auswahl; Rollen erreichen `cclom:general_keyword` per Konstruktion nie; Roundtrip inkl. Nesting testgesichert |
| **Presence-Extraktion** | `src/presence.js` nach Controller-Muster; Komponente wuchs trotz zweier neuer Features nur moderat |
| **Deployment** | Compose reicht jetzt ALLE dokumentierten Env-Vars durch (inkl. der vorher fehlenden Rate-/TTL-/Timeout-Vars); `${VAR:-}`-Footgun dokumentiert und durch `||`-Fallbacks entschärft; `.env.example` mit WLO-Key-Konvention |
| **Doku** | READMEs/Guides/CI-Kommentare abgeglichen, keine hartkodierten Suiten-Zahlen mehr; SEMANTISCHES-TAGGING dokumentiert die inhärente Yjs-Wrap-Grenze und die No-Retry-Entscheidung ehrlich |

Status der Audit-3-Punkte: **alle erledigt** (DOC-1, DEP-1 TipTap 3.27.3,
Express 5.2.1, F-T4 Early-Exit, F-T5 Extraktion, F-T6 `\n`-Regel). Bewusste
Tickets unverändert offen: extension-database/Redis (Multi-Instanz), echtes
Staging-Ticket für den Ticket-Login.

## 3. Offene Punkte (alle 🟢 Low/NIT)

- **F-T5 (Reprise): `md-collab-editor.js` wieder bei 503 Zeilen** — Rollen-
  Select/Chips + KI-Button/Status haben die Presence-Ersparnis überkompensiert.
  Nächste Extraktionskandidaten nach bewährtem Muster: Save-Bar-Wiring oder
  die Rollen-UI (Select + Chips) in ein eigenes Modul.
- **NIT: 🤖-Button für Read-only-Nutzer sichtbar** — `aiAvailable` hängt nur am
  Server-Key, nicht am Schreibrecht; ein Read-only-Klick erhält die (korrekt
  übersetzte) `no-write`-Meldung. Serverseitig sicher; UX-Feinschliff wäre,
  den Button zusätzlich an `canPersist` zu koppeln.
- **NIT: Kommentar-Drift in `config.js`** — der AI_API_KEY-Kommentar sagt
  „`|| undefined` guards …", der Code endet auf `|| null` (verhaltensgleich,
  Wortlaut anpassen).
- **DEP: marked 18.0.6** (Patch) — gefahrloses `npm update`.

## 4. Scorecard

```
Architektur 90 · Security 88 · Logik/Korrektheit 89 · Performance 84
API 86 · Frontend/UX 86 · Testing 90 · Wartbarkeit 83 · Dependencies 90 · DevOps 88
Overall: 88 (gewichtet)   — Verlauf: 78 → 81 → 86 → 88
```

Begründung: Testing +2 (271 Checks; KI-Suite deckt Security-Gate, Nebenläufigkeit
und Modell-als-Feind ab; Live-Verifikation gegen echtes Modell), Architektur +2
(Zwei-System-Trennung + KI-Kapselung), Wartbarkeit −1 (F-T5-Reprise).

## 5. Verdict: **Ship** ✅

Keine Findings oberhalb 🟢. Die App ist funktional komplett (Kollaboration,
Save-Regelung, zwei Tagging-Systeme, KI-Verschlagwortung, i18n, Docker-
Deployment), sicherheitsgeprüft über vier Audit-Runden und mit 271 automatischen
Checks abgesichert. Produktiv-Rahmenbedingungen unverändert: eine Instanz (oder
extension-redis), `TRUST_PROXY_HOPS` hinter Proxy, `.env`/Key nie ins Image,
`ALLOW_ANONYMOUS_EDIT` nur lokal.

*Erstellt mit `/better-coding-audit` gegen den Quellstand 2026-07-10; read-only.*
